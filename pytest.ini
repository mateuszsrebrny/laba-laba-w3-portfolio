[pytest]
pythonpath = .

markers =
    fast: marks tests as fast (API tests, mocked)
    slow: marks tests as slow (UI tests, OCR with real processing)

filterwarnings =
    ignore:'mode' parameter is deprecated:DeprecationWarning:easyocr.recognition
